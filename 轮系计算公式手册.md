# 轮系计算公式手册

> 来源：公式提取结果.csv
> 适用项目：gear_system（带轮传动系统）

---

## 目录

- [一、几何计算 (Geometry)](#一几何计算-geometry)
- [二、受力分析 (Analysis)](#二受力分析-analysis)
- [三、弧长系数 (ArcFactor)](#三弧长系数-arcfactor)
- [四、抖动计算 (Vibration)](#四抖动计算-vibration)
- [五、对齐度计算 (Alignment)](#五对齐度计算-alignment)
- [六、输入参数说明](#六输入参数说明)
- [七、计算流程总览](#七计算流程总览)
- [八、与项目代码映射](#八与项目代码映射)

---

## 一、几何计算 (Geometry)

**Excel Sheet**: Geometry（1446条公式）
**对应代码**: `services/gear_base.py`

### 1.1 带轮基础参数

| 物理量 | 符号 | 计算公式 | 说明 |
|--------|------|----------|------|
| 带轮编号 | B | 来自 Input!Bn | FAN / ALT / IDR / AC / TEN 等 |
| 中心X坐标 | C | `IF(B="NA"," ", IF(B="TEN", pivot_x, Input!Dn))` | 张紧轮取枢轴x坐标 |
| 中心Y坐标 | E | `IF(B="NA"," ", IF(B="TEN", pivot_y, Input!En))` | 张紧轮取枢轴y坐标 |
| 带轮类型 | M | 来自 Input!Hn | Grooved（槽轮）/ Flat（平轮） |
| 平轮节径 | G | `IF(M="Grooved","/", Input!Fn)` | 槽轮无此参数 |
| 节圆直径 | K | **平轮**: `G + 2×(带厚 + 衬厚)`<br>**槽轮**: `Input!Gn` | 节圆直径 mm |
| 外直径 | I (行5-14) | **槽轮**: `K + 2×衬厚`<br>**平轮**: `G + 2×带厚` | 外圆直径 mm |
| 速比 | U | `I_FAN / In` | 相对第一个带轮(FAN)的外直径比 |

**常量**：
- `带厚` = Input!H21
- `衬厚` = Input!H22
- `张紧轮枢轴x` = Geometry!C15
- `张紧轮枢轴y` = Geometry!E15

---

### 1.2 切点坐标（行18-27，与带轮一一对应）

每个带轮有两个切点：**入边切点** 和 **出边切点**。

| 物理量 | 符号 | 计算公式 | 说明 |
|--------|------|----------|------|
| 切点距离 | C | `sqrt((x₂-x₁)² + (y₂-y₁)²)` | 两个切点之间的距离（即皮带段长度） |
| 上切点角 | E | 见下方 | ASIN 公式，区分同/异类型 |
| 下切点角 | G | 见下方 | ACOS 公式，从水平轴算起 |
| 累计切点角 | I (行18-27) | `IF(G+E<0, 360+(G+E), G+E)` | 取 0°~360° |
| 对面切点角 | M | `IF(I<180, I+180, I-180)` | 对面 180° 的切点 |
| 前一带来角 | O | 上一带轮对面切点角 ± 180° | 见下方说明 |
| 包角中点 | K | 上下切点的平均角 | 见下方公式 |

#### 1.2.1 切点距离 C

```
若下一带轮为NA（最后一个带轮）：
  C = 与第一个带轮(FAN)的中心距
否则：
  C = 与下一带轮的中心距

C = sqrt((x_next - x_curr)² + (y_next - y_curr)²)
```

#### 1.2.2 上切点角 E（小角，用 ASIN）

```
规则：判断当前带轮与"对侧带轮"的类型是否相同

同类型（Grooved-Grooved 或 Flat-Flat）：
  E = DEGREES( ASIN( (K_curr - K_next) / (2 × C) ) )   [Grooved-Grooved]
  E = -DEGREES( ASIN( (K_curr - K_next) / (2 × C) ) )  [Flat-Flat]

不同类型（Grooved-Flat 或 Flat-Grooved）：
  E = DEGREES( ASIN( (K_curr + K_next) / (2 × C) ) )   [Grooved-Flat]
  E = -DEGREES( ASIN( (K_curr + K_next) / (2 × C) ) )  [Flat-Grooved]

注：最后一个带轮与第一个带轮(FAN)计算
```

#### 1.2.3 下切点角 G（大角，用 ACOS）

```
G = DEGREES( ACOS( (x_center - x_curr) / C ) )
若 y_center < y_curr： G = 360 - G

（即：从水平轴正方向算起，到两圆心连线的角度）
```

#### 1.2.4 累计切点角 I

```
I = G + E
若 I < 0： I = I + 360
（结果范围：0° ~ 360°）
```

#### 1.2.5 前一带来角 O

```
O[i] = P[i-1] + (0 if 同类型 else 180)

其中 P 为上一带轮的切点角（类似 K 的对面角？需确认）
第一个带轮的 O = P[last] + (0 if 同类型 else 180)
```

#### 1.2.6 包角中点 K

```
IF |I - O| < 180:
    K = (O + I) / 2
ELSE IF (I + O) > 360:
    K = (I + O) / 2 - 180
ELSE:
    K = (I + O) / 2 + 180
```

---

### 1.3 包角计算

| 物理量 | 符号 | 计算公式 | 说明 |
|--------|------|----------|------|
| Q值 | Q | `C × COS(RADIANS(E))` | 切点X投影 |
| 包角 | S | 见下方 | 皮带在带轮上的接触角 |
| 包角(小角) | U | `IF(S>180, S-180, 180-S)` | 取小于180°的角 |

#### 包角 S 公式

**槽轮 (Grooved)**：
```
diff = I_next - I_curr
IF diff < 0:
    S = diff + 360
ELSE:
    S = diff
```

**平轮 (Flat)**：
```
diff = I_next - I_curr
IF diff < 0:
    IF |diff| > 360:
        S = |diff| - 360
    ELSE:
        S = 360 - (diff + 360)
ELSE:
    S = 360 - diff
```

---

## 二、受力分析 (Analysis)

**Excel Sheet**: Analysis（5120条公式）
**对应代码**: `services/analysis.py`

> 行4~20：17个转速点的循环计算
> 每行 = 一个转速工况

### 2.1 输入数据（来自负载输入Sheet）

| 列 | 含义 |
|----|------|
| B | 序号 |
| C | 转速 (RPM) |
| D | 第1轮负载 |
| E | 第2轮负载 |
| F-O | 各带轮负载（10列，对应10个带轮位） |
| 单位切换 | 负载输入!F1 = "Kw" 或 "Nm" |

---

### 2.2 功率分配（G列 ~ O列）

```
IF 单位 = "Kw":
    P_i = 服务系数% × 速比_i × 负载功率_i
ELSE (单位 = Nm):
    P_i = 服务系数% × 速比_i × 负载转矩_i × π × RPM / 30000

其中：
  服务系数% = Input!Q12 / 100
  速比_i   = Input!J(5+i)
  负载     = 负载输入!F(3+i)
```

**合计功率 F**：
```
F = SUM(G:O)  =  所有带轮功率之和
```

---

### 2.3 皮带线速度（T列 / AW列）

```
v = π × D × n / 60000  [m/s]

其中：
  D = 节圆直径 (mm)，来自 $J$38
  n = 转速 (RPM)
```

---

### 2.4 圆周力 & 有效力

| 物理量 | 列 | 公式 | 说明 |
|--------|----|------|------|
| 总圆周力 | AX | `F = 1.25 × 1000 × Σ(P_i / 速比_i) / v` | 1.25 为安全系数 |
| 有效圆周力 | AY | `F_eff = (1 - 0.8 × arc_factor) × F` | 考虑弧长修正 |

---

### 2.5 带段拉力（U列 ~ Z列, AA~AD列）

每段皮带的拉力，用**余弦定理**计算：

```
F_segment = sqrt(F1² + F2² - 2 × F1 × F2 × COS(包角))

其中：
  F1, F2 = 相邻两个带轮的侧拉力
  包角    = 该段皮带对应的包角 (U$38, U$39...)
```

列对应关系：
- U = FAN 段拉力
- V = ALT 段拉力
- W = IDR 段拉力
- X = AC 段拉力
- Y = TEN 段拉力
- ... 依次类推

---

### 2.6 带轮侧拉力（AI列 ~ AR列, BN~CQ列）

每列一组（4列一组：紧边力+惯量力+修正+总力），共多组带轮。

**张紧轮修正**：
```
IF 带轮 = "TEN":
    F_total = (F_base + F_inertia + F_circum) × (1 + tension_factor / 100)
ELSE:
    F_total = F_base + F_inertia + F_circum

其中 tension_factor = Input!D23
```

---

### 2.7 惯量力

```
F_inertia = (惯量 × 速比 × π / 30) × RPM × 2000 / D

对应Excel: =($H$282*$W$46*PI()/30)*$AE$46*2000/$J$46
```

---

### 2.8 汇总区（行22~37）

| 行 | 物理量 | 公式/说明 |
|----|--------|-----------|
| 23 | 额定圆周力 | `Input!Q14 × (AY24/AV24)^(1/3)` |
| 24 | 总惯量功率 | `SUM(AV25:AV41)` |
| 25~37 | 各转速惯量 | `AV行 = AV_speed × AU_speed` |

---

## 三、弧长系数 (ArcFactor)

**Excel Sheet**: ArcFactor（508条公式）
**对应代码**: 包含在 `services/analysis.py` 中

### 3.1 查表结构

ArcFactor 是一张**二维线性插值表**，用于根据包角查询弧长修正系数。

```
槽轮查表入口：ArcFactor!B列(包角) → C列(系数)
平轮查表入口：ArcFactor!W列(包角) → X列(系数)
```

### 3.2 插值公式

```
系数 = 基准值 - (基准包角 - 当前包角) × 步长

步长：
  槽轮(C列): 0.002
  槽轮(E列): 0.002
  ... 各组步长不同，范围 0.002 ~ 0.0055
```

### 3.3 调用方式（Analysis 中使用）

```
IF 包角 > 240°:
    槽轮系数 = 1.135
    平轮系数 = 0.9
ELSE:
    系数 = OFFSET(ArcFactor!$C$2,
                  MATCH(INT(包角), ArcFactor!$B$3:$B$242, 0),
                  0)
```

---

## 四、抖动计算 (Vibration)

**Excel Sheet**: 抖动计算（3806条公式）
**对应代码**: `services/vibration.py`

> 按皮带段分组计算，每段包含相同结构的多列

### 4.1 皮带段几何参数

| 物理量 | 公式 | 说明 |
|--------|------|------|
| 中点X坐标 | `(x_pulley1 + x_pulley2) / 2` | 两端切点X的平均 |
| 中点Y坐标 | `(y_pulley1 + y_pulley2) / 2` | 两端切点Y的平均 |
| 切点距离 | `sqrt((x2-x1)² + (y2-y1)²)` | 皮带段长度 |
| 虚拟半径(侧1) | `sqrt((x_t1 - x_c1)² + (y_t1 - y_c1)²)` | 切点到圆心距离 |
| 虚拟半径(侧2) | `sqrt((x_t2 - x_c2)² + (y_t2 - y_c2)²)` | 切点到圆心距离 |

### 4.2 偏移切点（灵敏度分析）

```
沿包角方向偏移5%弧长后的切点位置：

1. 计算切点相对圆心的角度：θ = atan2(y_t - y_c, x_t - x_c)
2. 偏移5%：θ_offset = θ + 0.05 × 2π
3. 新切点坐标：
   x_new = x_c + R × cos(θ_offset)
   y_new = y_c + R × sin(θ_offset)

用途：计算皮带长度变化的灵敏度
```

---

## 五、对齐度计算 (Alignment)

**Excel Sheet**: 对齐度计算（1394条公式）
**对应代码**: `api/alignment.py`（`calc_contact_params()` + `calc_alignment_pairs()`）

> 对齐度计算分为两大步：
> 1. **Contact 参数计算**（K/J/L/M/N/P/O/Q/U）— 几何基础
> 2. **对齐度计算**（V/W/X/Y/BEA/Twist/Offset）— 实际对齐结果

---

### 5.1 输入参数

| 参数 | 符号 | 说明 |
|------|------|------|
| 中心高差 | S (centerHeightDiff) | 带轮中心沿轴向的偏移量 mm |
| 垂直度 | T (perpendicularity) | 带轮端面倾斜度 mm（倾斜大小） |
| 倾斜方向 | U (tiltAngle) | 用户输入的倾斜角度（优先用，否则用计算值） |
| 节圆直径 | D_j | 槽轮=Input!G，平轮=Input!F |
| Pitch-to-Effective | C4 | 槽轮到有效直径修正量 = 0.99 mm |
| Flat-to-Pitch | C5 | 平轮到节径修正量 = 1.5 mm |

---

### 5.2 Contact 参数计算（K/J/L/M/N/P/O/Q/U）

#### 5.2.1 类型系数 K

```
K = 1   （槽轮 Grooved）
K = -1  （平轮 Flat）
```

#### 5.2.2 基准直径 J

```
槽轮: J = 节圆直径 + Pitch_to_Effective
        = Input!G + 0.99
平轮: J = 平轮节径
        = Input!F
```

#### 5.2.3 有效直径 L

```
槽轮: L = J + 2 × C4   （C4 = Pitch_to_Effective = 0.99）
平轮: L = J + 2 × C5   （C5 = Flat_to_Pitch = 1.5）
```

#### 5.2.4 中心距 M

```
M[i] = sqrt( (x_next - x_curr)² + (y_next - y_curr)² )

最后一个带轮与第一个带轮(FAN)计算
```

#### 5.2.5 跨段长 N（皮带在两带轮间的有效长度）

```
half_diff = K_next × L_next / 2 - K_curr × L_curr / 2
N = sqrt( M² - half_diff² )

（即：皮带直线段长度，用勾股定理计算）
```

#### 5.2.6 出边切点角 P（Exit侧，相对水平轴）

```
1. 计算 base_angle（两圆心连线的角度）：
   IF y_next == y_curr:
       base_angle = π  (若 x_next < x_curr)
       base_angle = 0  (若 x_next > x_curr)
   ELSE:
       base_angle = ACOS(dx / M) × (1 if dy >= 0 else -1)

2. 计算 sign_term（切点偏移角）：
   kl = K_curr × K_next
   abs_half = |K_curr × L_curr/2 - K_next × L_next/2|
   ratio = abs_half / M

   IF kl > 0（同类型）:
       IF L_curr < L_next:
           acos_val = π - ACOS(ratio)
       ELSE:
           acos_val = ACOS(ratio)
   ELSE（不同类型）:
       acos_val = ACOS(ratio)

   sign_term = K_curr × acos_val

3. P = (base_angle - sign_term) × 180 / π   （转成角度）
```

#### 5.2.7 入边切点角 O（Entry侧）

```
O[i] = P[i-1] + (0  如果 K[i] == K[i-1]
                   180 如果 K[i] != K[i-1])

第一个带轮: O[0] = P[last] + (0  如果 K[0] == K[last]
                                 180 如果 K[0] != K[last])

（即：入边角 = 上一个带轮的出边角 ± 180°，取决于类型是否相同）
```

#### 5.2.8 包角 Q

```
Q = ( K × (P - O) )  mod  360
（取 0° ~ 360° 正值）

即：包角 = 出边角 - 入边角，再用 K 修正方向
```

#### 5.2.9 包角中点 U（Hubload方向）

```
槽轮 (K=1):  U = O - 180 + Q / 2
平轮 (K=-1): U = O - Q / 2 - 180

（即：包角的中点角度，Hubload合力方向）
```

---

### 5.3 倾斜投影计算（V/W/X/Y）

> 把带轮端面倾斜 T 投影到 Entry/Exit 侧的两个方向上

#### 5.3.1 V 值（Entry侧 Camber 投影）

```
V = T × [ sin(P) × sin(U) + cos(P) × cos(U) ]
  = T × cos(P - U)

（即：T在 P-U 方向的投影，余弦差公式）
```

#### 5.3.2 W 值（Exit侧 Camber 投影）

```
W = T × [ sin(O) × sin(U) + cos(O) × cos(U) ]
  = T × cos(O - U)

（即：T在 O-U 方向的投影）
```

#### 5.3.3 X 值（Entry侧 倾斜角投影）

```
X = T × [ -cos(P) × sin(U) + sin(P) × cos(U) ]
  = T × sin(P - U)

（即：T在 P-U 垂直方向的投影，正弦差公式）
```

#### 5.3.4 Y 值（Exit侧 倾斜角投影）

```
Y = T × [ -cos(O) × sin(U) + sin(O) × cos(U) ]
  = T × sin(O - U)
```

---

### 5.4 对齐度结果（BEA / Twist / Offset）

#### 5.4.1 BEA（Belt Entry Angle，切入角）

BEA 表示皮带进入带轮时的角度偏差。

**槽轮-槽轮 直接相邻**：
```
eff_y1 = S1 - L1/2 × sin(V1)      （当前带轮有效Y）
eff_y2 = S2 - L2/2 × sin(W2)      （下一带轮有效Y）

BEA = ATAN( (eff_y2 - eff_y1) / N ) × 180/π - X

其中：
  S1, S2 = 两个带轮的中心高
  L1, L2 = 两个带轮的有效直径
  V1     = 当前带轮Entry侧Camber投影
  W2     = 下一带轮Exit侧Camber投影
  N      = 跨段长
  X      = 当前带轮Entry侧倾斜角投影
```

**槽轮-平轮-槽轮（中间有一个平轮）**：
```
先算平轮Offset（AB），再用 AB 替代 S2 计算 BEA：

eff_y1 = S1 - L1/2 × sin(V1)
eff_y2 = AB - L_mid/2 × sin(W_mid)

BEA = ATAN( (eff_y2 - eff_y1) / N ) × 180/π - X
```

#### 5.4.2 Twist（扭转角）

Twist 表示两个带轮倾斜方向的差异。

```
Twist = K1 × V1 - K2 × W2

其中：
  K1, K2 = 两个带轮的类型系数（±1）
  V1     = 第一个带轮Entry侧Camber投影
  W2     = 第二个带轮Exit侧Camber投影
```

#### 5.4.3 Offset（平轮偏移量）

只有当中间有平轮时才计算 Offset。

```
Offset = S_next + N_flat × sin(X_flat) 
       + L_flat/2 × sin(V_flat) 
       - L_next/2 × sin(W_next)

其中：
  S_next    = 下一个槽轮的中心高
  N_flat    = 平轮的跨段长
  X_flat    = 平轮Entry侧倾斜角投影
  L_flat    = 平轮的有效直径
  V_flat    = 平轮Entry侧Camber投影
  L_next    = 下一个槽轮的有效直径
  W_next    = 下一个槽轮Exit侧Camber投影
```

---

## 六、输入参数说明

### 6.1 Input Sheet — 带轮参数

| 参数 | 列 | 说明 |
|------|----|------|
| 带轮编号 | B | FAN, ALT, IDR, AC, TEN, NA(空) |
| X坐标 | D | 带轮中心X |
| Y坐标 | E | 带轮中心Y |
| 平轮节径 | F | Flat类型带轮的节径 |
| 节圆直径 | G | Grooved类型带轮的节圆直径 |
| 带轮类型 | H | Grooved / Flat |
| 带厚 | H21 | 皮带厚度 |
| 衬厚 | H22 | 衬层厚度 |
| 服务系数 | Q12 | 百分比，如 90% |
| 张紧系数 | D23 | 百分比，张紧力增量 |
| 中心高差 | 对齐度S列 | 轴向偏移 mm |
| 垂直度 | 对齐度T列 | 端面倾斜 mm |

### 6.2 负载输入 Sheet

| 参数 | 说明 |
|------|------|
| 单位切换 | F1 = "Kw" 或 "Nm" |
| 转速列 | B列 | 各工况转速 (RPM) |
| 各带轮负载 | F~O列 | 每个带轮的功率/转矩 |

---

## 七、计算流程总览

```
输入参数
  │
  ├─ Input Sheet（带轮位置、直径、类型）
  ├─ 负载输入 Sheet（各转速下的负载）
  └─ 初始输入 Sheet（项目基本信息）
  │
  ▼
┌───────────────────────────────────┐
│  1. 几何计算 (Geometry)           │
│     ├─ 带轮基础参数（K/I/U）       │
│     ├─ 切点坐标（C/E/G/I/O）       │
│     └─ 包角（Q/S）                 │
└──────────────┬────────────────────┘
               │
               ▼
┌───────────────────────────────────┐
│  2. 对齐度 Contact 参数           │
│     ├─ 类型/直径（K/J/L）         │
│     ├─ 中心距/跨段长（M/N）       │
│     ├─ 切点角（P/O）              │
│     └─ 包角/中点（Q/U）           │
└──────────────┬────────────────────┘
               │
               ▼
┌───────────────────────────────────┐
│  3. 对齐度结果 (Alignment)        │
│     ├─ 倾斜投影（V/W/X/Y）        │
│     ├─ BEA（切入角）              │
│     ├─ Twist（扭转角）            │
│     └─ Offset（平轮偏移）         │
└──────────────┬────────────────────┘
               │
               ▼
┌───────────────────────────────────┐
│  4. 受力分析 (Analysis)           │
│     ├─ 功率分配                   │
│     ├─ 皮带线速度                 │
│     ├─ 圆周力 / 有效力            │
│     ├─ 弧长系数查表 (ArcFactor)   │
│     ├─ 带段拉力（余弦定理）        │
│     ├─ 带轮侧拉力                 │
│     └─ 惯量力修正                 │
└──────────────┬────────────────────┘
               │
               ▼
┌───────────────────────────────────┐
│  5. 抖动计算 (Vibration)          │
│     ├─ 皮带段中点坐标             │
│     ├─ 虚拟半径                   │
│     └─ 偏移切点灵敏度             │
└──────────────┬────────────────────┘
               │
               ▼
           报告输出
        （报告页 / Results）
```

---

## 八、与项目代码映射

| Excel Sheet | Python文件 | 类/函数 | 核心计算 |
|-------------|-----------|---------|----------|
| Geometry | `services/gear_base.py` | GearBaseService | calc_pitch_diameter<br>calc_outer_diameter<br>calc_speed_ratio<br>calc_tangent_point<br>calc_wrap_angle |
| Analysis | `services/analysis.py` | AnalysisService | calc_power_distribution<br>calc_belt_speed<br>calc_circumferential_force<br>calc_segment_force<br>calc_inertia_force |
| ArcFactor | `services/analysis.py` | ArcFactorService | lookup_arc_factor |
| 抖动计算 | `services/vibration.py` | VibrationService | calc_midpoint<br>calc_virtual_radius<br>calc_offset_tangent |
| 对齐度计算 | `api/alignment.py` | calc_contact_params() | K/J/L/M/N/P/O/Q/U 计算 |
| 对齐度计算 | `api/alignment.py` | calc_alignment_pairs() | V/W/X/Y/BEA/Twist/Offset |
| 报告页 | `services/report.py` | ReportService | generate_report |

---

## 附录：关键物理量单位

| 物理量 | 单位 |
|--------|------|
| 直径/距离 | mm |
| 角度 | 度（°） |
| 转速 | RPM |
| 线速度 | m/s |
| 功率 | kW |
| 力 | N |
| 转矩 | N·m |
